import 'encapsulated class.dart';

void main(){
  Data  obj = Data();
  // obj.myname ="Product 1";
  // obj.amount = 200;
  // obj.rate = 4.3;

  print(obj.myname);
  print(obj.amount);
  print(obj.rate);
}
///if we r using only getter properties in
///this class then Data class will be
///read only for this particular class